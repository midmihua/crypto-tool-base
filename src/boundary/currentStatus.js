const RequestBinanceData = require('../markets/binance');

const binance = new RequestBinanceData();

const getCurrectStatus = (coinsDataList) => {
    return coinsDataList
};

module.exports = getCurrectStatus;